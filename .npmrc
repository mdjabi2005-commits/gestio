# NODE_ENV=production vient du lanceur de session : npm en dérive omit=dev et
# n'installe alors AUCUNE devDependency (typescript, tsx, @types/node), ce qui
# fait échouer typecheck, lint, build et test sans dire pourquoi. include=dev
# neutralise omit quel que soit NODE_ENV. Ne pas supprimer.
include=dev
